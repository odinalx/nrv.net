<div class="page">
    <h1>Bienvenue sur {{title}}</h1>
    <p>{{description}}</p>
    {{#each items}}
        <div class="item">
            <h2>{{this.name}}</h2>
            <p>{{this.description}}</p>
        </div>
    {{/each}}
</div>